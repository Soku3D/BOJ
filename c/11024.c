#include <stdio.h>

int main(void)
{
	int T;
	scanf("%d", &T);
	while(T--)
	{
		int sum=0;
		char num[101];
		fgets(num, 100, stdin);
		for (int i = 0; i < num.size(); ++i)
		{
			
		}
	}
	return 0;
}